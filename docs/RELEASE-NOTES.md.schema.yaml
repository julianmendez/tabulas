---
$schema: http://json-schema.org/draft-06/schema#
title: release-notes
type: array
items:
  type: array
  items:
    type: object
    additionalProperties: false
    properties:
      version:
        type: string
      date:
        type: string
      new_features:
        type: array
        items:
          type: string
      deprecated:
        type: array
        items:
          type: string
      bug_fixes:
        type: array
        items:
          type: string
      new_types:
        type: array
        items:
          type: string
      new_readers:
        type: array
        items:
          type: string
      new_writers:
        type: array
        items:
          type: string
      build:
        type: string
      release:
        type: string
